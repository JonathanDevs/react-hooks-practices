import React from 'react';

const ThemeContext = React.createContext(null);
// ThemeContext forma visual de presentar los cambios que queramos transmitir a toda nuestra app 

export default ThemeContext;

